# ✅ Firewall Detection & Evasion Tool - Gereksinimler

# 📌 Bu Python projesi yalnızca standart kütüphanelerle çalışır.
# subprocess, argparse, re, socket, json, logging, datetime gibi modüller zaten Python ile birlikte gelir.

# 📦 Ekstra pip paketi kurulumu gerekmez.

# ⚠️ Ancak sisteminizde Nmap kurulu olmalıdır. Aşağıdaki komutla yükleyebilirsiniz:

# Linux (Debian/Ubuntu):   sudo apt update && sudo apt install nmap
# macOS (Homebrew):        brew install nmap
# Windows:                 https://nmap.org/download.html adresinden indirin ve PATH'e ekleyin

# 🔄 Kurulum Adımları (önerilen):
# git clone https://github.com/hizir777/nmap_evasion.git
# cd firewall-evasion-tool
# python -m venv venv (sanal ortam kurulması tavsiye edilir.)
# source venv/bin/activate     (Windows: venv\Scripts\activate)
# pip install -r requirements.txt

# ✅ Projeyi çalıştırmak için:
# python main.py hedefsite.com
